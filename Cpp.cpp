#include <iostream> //includes the console in and out functions (for console apps
#include <stdio.h> //includes the fopen and printf functions (mostly for debugging)
#include <string> //includes the string functionality
int main() {
    std::cout << /*Console out*/;
    std::getline (std::cin,/*a string*/)
}
